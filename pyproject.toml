[tool.poetry]
name = "schedule-generator"
version = "0.1.0"
description = ""
authors = ["Tikhonov Maxim"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
